import { useRef, useState, useCallback } from "react"

export const IndexPage = () => {
   
    return (
        <div style={{ height: '500px' }}>
            sdfsfdsdf
           
        </div>
    );
}